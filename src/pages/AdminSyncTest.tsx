import React from 'react';
import { SyncPaymentMethodTest } from '@/components/admin/SyncPaymentMethodTest';

const AdminSyncTest = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-background to-muted/20 flex items-center justify-center p-4">
      <SyncPaymentMethodTest />
    </div>
  );
};

export default AdminSyncTest;
